import { ReactiveTitleEmitter } from "./ReactiveTitle.emitter";
import { ReactiveTitleListener } from "./ReactiveTitle.listener";

export const ReactiveTitle = {
  Emitter: ReactiveTitleEmitter,
  Listener: ReactiveTitleListener,
};
